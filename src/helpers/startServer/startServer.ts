// @ts-nocheck
// import { log, save } from '../../logger';

const startServer = async () => {
  console.log(`running...`);

  // ---

  // log.info('text info!', { meta1: 'meta1' }, save(111));
  // log.warn('text warn!', save(222));
  // log.error('text error!', save(333));

  // ---
};

export default startServer;
